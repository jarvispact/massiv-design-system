import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="Docs/Typography" />

# Typography

For Typography there is a `<Heading />` and a `<Text />` Component available. You can control the rendered dom element with the `as` property. For example: `<Text as="label">i am a label</Text>`. The styling can be controlled with the `variant` property.

## Heading

<Canvas>
  <Story id="docs-typography--heading" />
</Canvas>

## Text

<Canvas>
  <Story id="docs-typography--text" />
</Canvas>
